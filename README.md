# TimeMQTT 
Script for using the MQTT (Message Queuing Telemetry Transport) protocol to transmit GNSS data. The MQTT protocol, commonly used in the context of IoT (Internet of Things), was used to experimentally transmit GNSS data from the UTC (INXE) publisher to different subscribers.
